function CompressBoxTwice(boxes) {
 boxes.forEach(boxes => console.log(boxes);) //O(n)
 boxes.forEach(boxes => console.log(boxes);) //O(n)
}

//Rule1: Taking Worst case we will have - O(n), because of for each loop.
// So, O(2n)
//Rule2: Drop the constants.

// O(n)